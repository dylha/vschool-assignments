function arrFilter(str) {
    const final = [];
    const arr = str.split(" ")
    const lengthArr = arr.map(function(word){
        return word.lengthArr
    })


}

arrFilter("hello world I'm an array") // returns ["hello", "world", "array"]

modules.export = arrFilter;
