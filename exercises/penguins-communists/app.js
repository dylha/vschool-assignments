class BigBoys {
    constructor(name, allegiance, size) {
        this.name = name;
        this.allegiance = allegiance;
        this.size = size;
    }
}

const penguins = new BigBoys("Penguins", "Antarctica", 1,000,000);
const communists = new BigBoys("Communists", "The Motherland", 1,000,000);







const coinToss = (min, max) => Math.floor((Math.random() * max) + min);
if (coinToss(1, 2) === 1) {
    console.log(`\nHeads!\n`);
} else {
    console.log(`\nTails...\n`);
}







const sendNuke = (name, onHit, onMiss) => {
    // party. hit, miss
}









































































